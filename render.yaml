services:
  - type: web
    name: ecogrow
    env: docker
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: DB_HOST
        fromDatabase:
          name: ecogrow-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: ecogrow-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: ecogrow-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: ecogrow-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: ecogrow-db
          property: password

databases:
  - name: ecogrow-db
    plan: free
