services:
  - type: web
    name: ecogrow
    env: docker
    plan: free
    envVars:
      - key: PORT
        value: 8080
      - key: DATABASE_URL
        fromDatabase:
          name: ecogrow-db
          property: connectionString

databases:
  - name: ecogrow-db
    plan: free
